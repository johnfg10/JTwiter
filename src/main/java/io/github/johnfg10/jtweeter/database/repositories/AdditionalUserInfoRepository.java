package io.github.johnfg10.jtweeter.database.repositories;

import io.github.johnfg10.jtweeter.database.model.User;
import org.springframework.data.jpa.repository.JpaRepository;

import java.util.Optional;

/*
public interface AdditionalUserInfoRepository extends JpaRepository<AdditionalUserInfoRepository, Integer> {
    Optional<AdditonalUserInfo> findByUser(User user);
}
*/
